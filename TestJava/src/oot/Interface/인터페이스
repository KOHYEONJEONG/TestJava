접근권한 interface 인터페이스명{
      [내용]
       - 필드
       - 접근권한 반환타입 메소드명(인자타입 인자명);(내용x)
}
------------------------------------------------
[자동생성 부분]
- 필드     : public static final을 입력 안하고 타입만 입력해도 interface페이지에서는 자동으로 인식함!

- 메소드   : public abstract 안 붙여도 자동으로 인식

------------------------------------------------
[인터페이스와 구현클래스 명명규칙]
1. 인터페이스명 앞에 I를 붙인다. <-인터페이스
2. 참조하는 클래스명 뒷부분에 impl을 붙인다. <-구현클래스